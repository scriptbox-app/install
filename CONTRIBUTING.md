# Contributing

Use PHP 8.3+ and Node 24. Preserve unrelated changes, add a failing test before behavior changes, and run [release verification](docs/release.md). Do not add arbitrary commands, URLs, SQL/filesystem MCP tools, package hooks, secrets, or unbounded operations. Protocol changes must update installer, UI, API, OpenAPI, and MCP contracts together.
